import { popUpBalloonsGameDefaultSettings } from 'shared/utils/data';
import example1Png from '../img/example1.png';

const settingTooltipFields = [
  `Enter number (from 1, to ${popUpBalloonsGameDefaultSettings.characterRowCount.max}) that defines how many characters there will be in the rows`,
];

export { settingTooltipFields };
